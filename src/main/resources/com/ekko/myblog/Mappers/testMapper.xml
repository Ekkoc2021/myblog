<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ekko.myblog.Mappers.testMapper">
    <!--配置环境的时候用于测试的mapper-->
    <select id="selectAll" resultType="test">
        select * from test;
    </select>
</mapper>